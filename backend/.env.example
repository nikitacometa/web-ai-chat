# Backend Environment Variables Example

# Used by the admin routes to authenticate requests
ADMIN_TOKEN="REPLACE_WITH_A_STRONG_UNIQUE_ADMIN_TOKEN"

# Supabase Project URL
SUPABASE_URL="YOUR_SUPABASE_URL_HERE"

# Supabase Service Role Key (use with caution, prefer RLS and anon key if possible for some operations)
# Alternatively, for less privileged backend operations, you might use the anon key.
SUPABASE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY_HERE"

# OpenAI API Key for image generation
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: Base URL for constructing Twitter avatar images if you want to customize it
# Default in current code is https://unavatar.io/twitter/{handle}
# AVATAR_BASE_URL="https://unavatar.io/twitter/{handle}"

# Algorand Configuration (Backend - for Payouts & Verification)
# These are used by backend services like the payout job.
ALGOD_NODE_SERVER="https://testnet-api.algonode.cloud" # Algorand node server URL
ALGOD_NODE_TOKEN="" # Algorand node API token (often empty for public nodes)
ALGOD_NODE_PORT="443" # Algorand node port

# Game Wallets
HOT_WALLET_MNEMONIC="YOUR_25_WORD_ALG звездный_HOT_WALLET_MNEMONIC_FOR_PAYOUTS" # Required for real payouts
GAME_TREASURY_ADDRESS="YOUR_ALGORAND_GAME_TREASURY_ADDRESS_HERE" # Address that receives user bets (for verification)

# Game Mechanics Timeouts (Optional - defaults exist in config.py)
ROUND_INACTIVITY_TIMEOUT_SEC=1200 # (Default: 20 minutes) Inactivity before round ends
MAX_ROUND_DURATION_SEC=86400    # (Default: 24 hours) Absolute max round duration
BET_TIME_EXTENSION_SEC=60       # (Default: 1 minute) Time added per bet 