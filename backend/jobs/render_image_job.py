# Background job for rendering images via OpenAI

# async def run_render_image_job(job_id: str, round_id: int, prompt: str):
#     # calls openai_service.generate_image
#     # updates supabase_service with new image_url
#     pass 